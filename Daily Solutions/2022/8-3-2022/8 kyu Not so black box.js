// 8 kyu Not so black box
blackBox();
JSopenSesame();

// The solution is to console.log(blackBox.toString()) to see the source code.
// If you're curious, you can do the same to JSopenSesame() and read its code
// to solve it without calling JSopenSesame()
